import express from 'express'
import { Request, Response } from 'express'
// import {  subscribeEvent } from '../controllers/app_event'

export default (router: express.Router) => {
    // router.post('/order/add', addOrder);
}